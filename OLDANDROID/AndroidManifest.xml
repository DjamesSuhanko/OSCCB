<?xml version="1.0"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.suhanko.osccb" android:versionCode="" android:versionName="">

    <!-- Versões alvo e mínima que a Play vai ler -->
    <uses-sdk android:minSdkVersion="28" android:targetSdkVersion="35"/>

    <!-- Permissões que seu app realmente usa -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application android:label="OSCCB">
        <activity android:name="org.qtproject.qt.android.bindings.QtActivity" android:configChanges="orientation|keyboardHidden|screenSize|locale|layoutDirection|fontScale|screenLayout|smallestScreenSize|uiMode" android:screenOrientation="sensorLandscape" android:launchMode="singleTop" android:label="OSCCB">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
